name: CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  install-dependencies:
    runs-on: ubuntu-latest  # Or use your preferred OS environment

    steps:
      - name: Check out the code
        uses: actions/checkout@v3

      # Set up Python 3.12
      - name: Set up Python 3.12
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'  # You can change to '3.11' if needed

      # Upgrade pip to the latest version
      - name: Upgrade pip
        run: |
          python -m pip install --upgrade pip

      # Upgrade setuptools to the latest version
      - name: Upgrade setuptools
        run: |
          pip install --upgrade setuptools

      # Install wheel to avoid build issues
      - name: Install wheel
        run: |
          pip install wheel

      # Install numpy (try installing the precompiled binary first)
      - name: Install NumPy
        run: |
          pip install numpy==1.23.5 --only-binary :all:  # Attempt to install a precompiled binary

      # Additional steps if you want to run tests
      - name: Run tests
        run: |
          # Example of running tests (adjust based on your testing framework)
          pytest
